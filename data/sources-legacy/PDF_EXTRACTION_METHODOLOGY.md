# Методология извлечения данных из PDF

> **Руководство по извлечению технической информации из PDF-каталогов и стандартов**

[🏠 На главную](../README.md) | [✅ Статус извлечения](EXTRACTION_STATUS.md)

---

## Содержание

1. [Обзор](#обзор)
2. [Инструменты](#инструменты)
3. [Процесс извлечения](#процесс-извлечения)
4. [Качество данных](#качество-данных)
5. [Примеры извлечения](#примеры-извлечения)

---

## Обзор

Данный документ описывает процесс извлечения технической информации из PDF-файлов каталогов производителей и стандартов ГОСТ. Извлеченные данные преобразуются в структурированные Markdown-документы для использования в базе данных.

### Цели извлечения

1. **Сохранение информации** - перевод информации из PDF в удобный для поиска формат
2. **Структурирование данных** - организация информации в логические разделы
3. **Доступность** - обеспечение быстрого доступа к техническим данным
4. **Кросс-ссылки** - создание связей между разными источниками

---

## Инструменты

### Python библиотеки

#### pypdf (ранее PyPDF2)
```python
from pypdf import PdfReader

reader = PdfReader("bearing_catalog.pdf")
page = reader.pages[0]
text = page.extract_text()
```

**Применение:**
- Извлечение текста из PDF
- Доступ к метаданным документа
- Обработка страниц

**Примечание:** `pypdf` - это современная версия библиотеки PyPDF2. Для новых проектов рекомендуется использовать именно pypdf.

**Ограничения:**
- Не работает с отсканированными документами
- Может некорректно извлекать текст из таблиц
- Не распознает структуру документа

#### pdfplumber
```python
import pdfplumber

with pdfplumber.open("bearing_catalog.pdf") as pdf:
    page = pdf.pages[0]
    text = page.extract_text()
    tables = page.extract_tables()
```

**Применение:**
- Извлечение текста с сохранением структуры
- Извлечение таблиц
- Определение координат текстовых блоков

**Преимущества:**
- Лучше работает с таблицами
- Сохраняет структуру документа
- Может определять границы таблиц

#### Tabula-py
```python
import tabula

# Извлечение всех таблиц с страницы
tables = tabula.read_pdf("bearing_catalog.pdf", pages='1')

# Извлечение конкретной области (координаты в пунктах от верхнего левого угла)
# Пример: top=100, left=50, bottom=400, right=550
table = tabula.read_pdf(
    "bearing_catalog.pdf",
    area=[100, 50, 400, 550],  # [top, left, bottom, right] в пунктах
    pages='1'
)
```

**Применение:**
- Специализированное извлечение таблиц
- Преобразование в DataFrame
- Экспорт в CSV/Excel

**Примечание:** Координаты area определяются в пунктах (points) от верхнего левого угла страницы. Для определения координат можно использовать инструмент Tabula GUI или PDF-просмотрщик с линейкой.

#### OCR (Tesseract + pytesseract)
```python
from pdf2image import convert_from_path
import pytesseract

# Конвертация PDF в изображения
images = convert_from_path('scanned_document.pdf')

# OCR для каждой страницы
for image in images:
    text = pytesseract.image_to_string(image, lang='rus')
```

**Применение:**
- Отсканированные документы
- PDF с изображениями вместо текста
- Низкокачественные PDF

**Требования:**
- Установленный Tesseract OCR
- Хорошее качество исходного документа
- Возможны ошибки распознавания

---

## Процесс извлечения

### Этап 1: Анализ документа

**Действия:**
1. Открыть PDF и оценить структуру
2. Определить тип содержимого:
   - Текстовый PDF (можно извлекать напрямую)
   - Отсканированный PDF (требуется OCR)
   - Смешанный (текст + изображения)
3. Идентифицировать разделы:
   - Оглавление
   - Технические характеристики
   - Таблицы размеров
   - Обозначения
   - Примеры применения

**Пример анализа для "Обозначения подшипников NSK.pdf":**
- Тип: Текстовый PDF с таблицами
- Структура:
  - Введение (стр. 1-2)
  - Базовые обозначения (стр. 3-5)
  - Префиксы (стр. 6-8)
  - Суффиксы (стр. 9-15)
  - Примеры (стр. 16-18)

### Этап 2: Извлечение данных

#### Для текстовых PDF

```python
import pdfplumber
import re

def extract_bearing_designations(pdf_path):
    """
    Извлекает информацию об обозначениях подшипников
    """
    designations = {
        'prefixes': [],
        'suffixes': [],
        'examples': []
    }
    
    with pdfplumber.open(pdf_path) as pdf:
        for page in pdf.pages:
            # Извлечение текста
            text = page.extract_text()
            
            # Извлечение таблиц
            tables = page.extract_tables()
            
            # Обработка таблиц
            for table in tables:
                if is_prefix_table(table):
                    designations['prefixes'].extend(
                        parse_prefix_table(table)
                    )
                elif is_suffix_table(table):
                    designations['suffixes'].extend(
                        parse_suffix_table(table)
                    )
    
    return designations

def parse_suffix_table(table):
    """
    Парсинг таблицы суффиксов
    """
    suffixes = []
    
    # Пропускаем заголовок
    for row in table[1:]:
        if len(row) >= 2 and row[0] and row[1]:
            suffixes.append({
                'suffix': row[0].strip(),
                'description': row[1].strip(),
                'notes': row[2].strip() if len(row) > 2 else ''
            })
    
    return suffixes
```

#### Для отсканированных PDF

```python
from pdf2image import convert_from_path
import pytesseract
from PIL import Image

def extract_from_scanned_pdf(pdf_path):
    """
    Извлечение текста из отсканированного PDF
    """
    # Конвертация PDF в изображения
    images = convert_from_path(
        pdf_path,
        dpi=300  # Высокое разрешение для лучшего OCR
    )
    
    text_content = []
    
    for i, image in enumerate(images):
        # Предобработка изображения (опционально)
        image = preprocess_image(image)
        
        # OCR
        text = pytesseract.image_to_string(
            image,
            lang='rus+eng',  # Русский и английский
            config='--psm 6'  # Режим: блок текста
        )
        
        text_content.append({
            'page': i + 1,
            'text': text
        })
    
    return text_content

def preprocess_image(image):
    """
    Предобработка изображения для улучшения OCR
    """
    # Конвертация в градации серого
    image = image.convert('L')
    
    # Увеличение контраста
    # (дополнительные операции по необходимости)
    
    return image
```

### Этап 3: Структурирование данных

**Преобразование извлеченных данных в Markdown:**

```python
def create_markdown_document(designations, output_path):
    """
    Создание Markdown-документа из извлеченных данных
    """
    md_content = []
    
    # Заголовок
    md_content.append("# Система обозначений подшипников NSK\n")
    md_content.append("[🏠 На главную](../../../README.md)\n")
    md_content.append("---\n\n")
    
    # Префиксы
    md_content.append("## Префиксы\n\n")
    md_content.append("| Префикс | Описание | Примечания |\n")
    md_content.append("|---------|----------|------------|\n")
    
    for prefix in designations['prefixes']:
        md_content.append(
            f"| {prefix['prefix']} | {prefix['description']} | "
            f"{prefix.get('notes', '')} |\n"
        )
    
    md_content.append("\n")
    
    # Суффиксы
    md_content.append("## Суффиксы\n\n")
    md_content.append("| Суффикс | Описание | Примечания |\n")
    md_content.append("|---------|----------|------------|\n")
    
    for suffix in designations['suffixes']:
        md_content.append(
            f"| {suffix['suffix']} | {suffix['description']} | "
            f"{suffix.get('notes', '')} |\n"
        )
    
    # Запись в файл
    with open(output_path, 'w', encoding='utf-8') as f:
        f.writelines(md_content)
```

### Этап 4: Проверка качества

**Контрольные точки:**
1. Все таблицы извлечены корректно
2. Нет пропущенных разделов
3. Числовые данные соответствуют оригиналу
4. Структура документа логична
5. Все ссылки работают

**Чек-лист проверки:**
- [ ] Извлечены все таблицы
- [ ] Проверены ключевые числовые значения (выборочно)
- [ ] Сохранена иерархия разделов
- [ ] Добавлены перекрестные ссылки
- [ ] Добавлены примеры использования
- [ ] Указан источник и дата извлечения

---

## Качество данных

### Уровни качества

**⭐⭐⭐⭐⭐ Отлично (95-100% точность)**
- Все данные извлечены без ошибок
- Таблицы корректно отформатированы
- Сохранена полная структура документа
- Добавлены практические примеры
- **Метрика:** <5% данных требуют ручной корректировки

**⭐⭐⭐⭐ Хорошо (85-94% точность)**
- Основные данные извлечены корректно
- Минимальные погрешности в форматировании
- Требуется небольшая ручная доработка
- **Метрика:** 5-15% данных требуют ручной корректировки

**⭐⭐⭐ Удовлетворительно (70-84% точность)**
- Данные извлечены, но требуют проверки
- Некоторые таблицы нуждаются в корректировке
- Необходима ручная проверка критичных данных
- **Метрика:** 15-30% данных требуют ручной корректировки

**⭐⭐ Плохо (50-69% точность)**
- Частичное извлечение
- Много ошибок в данных
- Требуется повторное извлечение с другими настройками
- **Метрика:** 30-50% данных требуют ручной корректировки

**⭐ Очень плохо (<50% точность)**
- Извлечение не удалось
- Требуется полностью ручная обработка
- **Метрика:** >50% данных требуют ручной корректировки или полная переделка

### Критерии оценки

1. **Полнота** - извлечены ли все разделы?
2. **Точность** - соответствуют ли числа оригиналу?
3. **Структура** - сохранена ли иерархия?
4. **Читаемость** - удобен ли документ для использования?

---

## Примеры извлечения

### Пример 1: Обозначения подшипников NSK

**Исходный файл:** `sources/catalogs/Oboznacheniya-podshipnikov-NSK.pdf` (1014 KB)

**Целевой документ:** `docs/bearings/brands/nsk_designation_system.md`

**План извлечения:**

1. **Структура исходного PDF:**
   - Введение (1-2 стр.)
   - Базовые обозначения (3-6 стр.)
   - Префиксы (7-10 стр.)
   - Суффиксы (11-20 стр.)
   - Примеры (21-23 стр.)

2. **Целевая структура Markdown:**
   ```markdown
   # Система обозначений подшипников NSK
   
   ## Введение
   ## Базовые обозначения
   ### Структура обозначения
   ### Примеры базовых обозначений
   
   ## Префиксы
   ### Таблица префиксов
   ### Примеры использования префиксов
   
   ## Суффиксы
   ### Уплотнения и защиты
   ### Материалы и покрытия
   ### Точность и зазоры
   ### Смазка
   ### Специальные конструкции
   
   ## Практические примеры
   ### Пример 1: Расшифровка 6205ZZ
   ### Пример 2: Расшифровка 6309DDU
   ```

3. **Метод извлечения:**
   - Использовать pdfplumber для извлечения таблиц
   - Текстовые разделы извлекать с помощью PyPDF2
   - Вручную добавить практические примеры

4. **Ожидаемое качество:** ⭐⭐⭐⭐⭐ (Отлично)

### Пример 2: Общий каталог SKF (частичное извлечение)

**Исходный файл:** `sources/catalogs/Obshchii_-katalog-podshipnikov-SKF.pdf` (20 MB)

**Целевые документы:**
- `docs/bearings/types/skf_ball_bearings.md` - Шариковые подшипники
- `docs/bearings/types/skf_roller_bearings.md` - Роликовые подшипники
- `docs/bearings/tables/skf_dimensions.md` - Таблицы размеров

**План извлечения:**

1. **Приоритет:** Извлечь только ключевые разделы (не весь каталог)
   - Таблицы размеров для популярных серий (60, 62, 63)
   - Таблицы грузоподъемности
   - Системы обозначений (если отличаются от стандартных)

2. **Метод:**
   - Использовать tabula-py для извлечения таблиц размеров
   - Сохранить в CSV, затем конвертировать в Markdown
   - Добавить перекрестные ссылки на другие документы

3. **Ожидаемое качество:** ⭐⭐⭐⭐ (Хорошо)
   - Таблицы требуют ручной проверки
   - Некоторые специальные символы могут быть искажены

---

## Рекомендации

### Приоритизация

1. **Высокий приоритет:** Документы с уникальной информацией
   - Обозначения производителей (NSK, SKF)
   - Таблицы соответствий
   - Технические характеристики

2. **Средний приоритет:** Дополнительная информация
   - Примеры применения
   - Руководства по монтажу
   - Рекомендации по смазке

3. **Низкий приоритет:** Общая информация
   - Рекламные материалы
   - Информация о компании
   - Истории успеха

### Оптимизация процесса

1. **Автоматизация:** Создать скрипты для типовых задач
2. **Шаблоны:** Использовать единые шаблоны Markdown
3. **Проверка:** Всегда проверять критичные данные вручную
4. **Документирование:** Фиксировать статус извлечения в EXTRACTION_STATUS.md

---

## Заключение

Извлечение данных из PDF - это баланс между автоматизацией и качеством. Для критичных технических данных всегда необходима ручная проверка, особенно для:
- Размеров подшипников
- Грузоподъемности
- Предельных скоростей
- Допусков

Используйте данную методологию как отправную точку и адаптируйте под конкретные документы.

---

**Полезные ссылки:**
- [Статус извлечения данных](EXTRACTION_STATUS.md)
- [Библиография и источники](literature.md)
- [Индекс извлеченных знаний](../docs/EXTRACTED_KNOWLEDGE_INDEX.md)

---

**Дата создания:** 2025-12-29  
**Автор:** Команда разработки Baza  
**Статус:** Руководство для разработчиков
